# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 07 — Frontend Testing & Quality · **4/6 topics taught (7.1 ✅, 7.2 ✅, 7.3 ✅, 7.4 ✅)** |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); Module 06 ✅ (7/7 + milestone, 2026-07-24). **Module 07 started 2026-09-14 after a ~7-week break — 7.1 + 7.2 taught 2026-09-14, 7.3 RTL 09-14 → 09-15, 7.4 E2E (Playwright) 2026-09-17. Next: 7.5 Accessibility Testing.** |
| **Week Goal** | Teach **7.5 Accessibility Testing**, then **7.6 Web Performance & Core Web Vitals**, then set the **Module 07 milestone**. The student has still never *run* a test (7.1/7.2 homework skipped; 7.3 = written unrun example; 7.4 had no hands-on — its "passes headless" exit bar is folded into the milestone) — the milestone is the first real, executed test code, so review it closely. Cadence: architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last question pushed for the who-pays ending; **one-line repeat is optional** (see below). |
| **Last Session Date** | 2026-09-17 |
| **Last Session Summary** | **7.4 E2E Testing (Playwright) — two parts, mechanics included.** Keep-or-drop on the repeat step: student didn't answer → mentor declared it **optional** unless told otherwise; no objection. **Part 1:** E2E = the only layer that tests the seams (build, routing, real browser, page handoffs) → a 1–3 critical-journey budget (GitHub sign-in → PR → merge; Netflix sign-in → profile → play); config testing the **production build** via `webServer` (`build && preview`), `forbidOnly`, CI retries, `trace: 'on-first-retry'`; Vitest-picks-up-`e2e/**` exclude gotcha; role-first locators (RTL habit transfers); **actionability auto-wait + web-first assertions = condition waits, no sleeps**; Repo Pulse search → dashboard test. **Guiding question** (`expect(await …isVisible()).toBe(true)` flaky on CI 1 in 5): Q1 **pass on core** (isVisible = right now) — slips: said toBeVisible waits until *attached* (it's *visible*, with retry + timeout), "on CI the page is running" explained nothing; Q2 "no idea" → short-label MC → picked only **E** (laptop half), missed A/B/D (shared CPUs, parallel workers, variable API) → own sentence had the mechanism right → pass. **Part 2:** `storageState` via setup project + `dependencies`; browser isolated but **server shared** → unique data per test (`workerIndex` + timestamp); real backend vs `page.route` table (GitHub 60 req/h per IP rate-limit); default = real for own backend, fake third parties + scheduled smoke test; **Next.js caveat: `page.route` can't see server-side fetch**; flakiness table (incl. timezone/locale, animations); retries → "flaky" = bug ticket; trace viewer; GitHub Actions workflow + mentor self-correction (github-only reporter produces no HTML report to upload → use both reporters). **Say-back Q1** (60 E2E tests): pass unaided — slow + network/service dependent → critical flows only (login, payment); missing the "component tests for the rest" alternative and flake compounding. **Q2** (fake GitHub everywhere): **pass unaided, who-pays volunteered, verb direction correct** ("users *pay for* the bug") + volunteered the scheduled smoke-test fix; missing second payer (team learns from users) and the "only/with no real check" qualifier. English: subject–verb again (*test cause → causes*, *test don't → doesn't*), *depent*, *crititcal*, double *only*, *form → from*, *sometime → on a schedule*, *thate slower*. |
| **Previous Session Summary** | **7.3 Component Testing (RTL)** — two parts with mechanics: public-surface testing, jsdom/jest-dom setup, getBy/queryBy/findBy, role-first priority (failed `getByRole` = free a11y check), `userEvent`; `findBy`/`waitFor` traps, MSW at the network vs `vi.mock` the hook, fresh `QueryClient` + per-query `retry` override. Worked example on the student's real work Role & Permission admin (Thai comments, unrun) surfaced 3 a11y bugs. Q1 (`setTimeout(500)`) concept pass, precision miss; Q2 (MSW vs mock hook) short-label MC 4/4 → both payers, verb inverted ("users cause bugs"). |
| **Homework** | None open. 7.1 test plan + 7.2 hands-on **skipped at the student's request (2026-09-14)** — not to be re-collected. None set for 7.3 (written worked example instead) or 7.4 (its exit bar — one real journey passing headless — is folded into the Module 07 milestone). |
| **Homework Status** | ⏭️ Skipped (7.1, 7.2) · none set (7.3, 7.4). Consequence: Module 07 milestone = first executed test code → review closely (tests assert outcomes not calls; boundary fixes aren't special-cased; role-first queries; network faked at the boundary; E2E uses web-first assertions — no sleeps or snapshot checks — unique data per test, passes headless in CI with the report uploaded). |
| **Next Action** | **Teach 7.5 Accessibility Testing:** automated vs manual a11y (automation catches only a fraction — keyboard + screen-reader passes still needed); `axe-core` in component tests and in E2E (`@axe-core/playwright`, scoping + disabling rules deliberately); role-first queries as a continuous a11y check (callback to 7.3); keyboard-flow tests (Tab order, modal focus trap, focus return); limits of the Lighthouse a11y score; where to gate in CI. **Natural subject:** the 3 real a11y bugs found in the student's work roles admin (unlinked `Form.Item` labels; duplicate accessible names on group checkboxes → `role="group"` + `aria-labelledby`; skeleton without `aria-busy`) — ask which ones axe would catch vs only a role query / manual pass. Then 7.6, then set the milestone (Repo Pulse at production quality: unit + component + one E2E flow passing headless in CI + a11y audit + Lighthouse pass + written testing strategy). **Cadence:** architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last pushed for who-pays; **one-line repeat optional** (declared 2026-09-17, no objection). **Articulation playbook:** fill-in-the-blank → **short-label** MC if stalled → say it back. **Watch-items:** (a) MC picks tend to be *partial* (7.4: 1 of 4 correct letters) — nudge for the rest; (b) who-pays **improving** (7.4 Q2 unaided, verb direction correct) — now push for the **second payer** (team) and qualifiers like "only"; (c) vague mechanism ("the page is running") — ask "what is concretely different?"; (d) waits mostly landed — check *visible vs attached*; (e) when pushing back, **offer the alternative** (7.4 Q1 omitted it); (f) English: subject–verb agreement (most persistent), spelling (*depent*, *crititcal*), double *only*. |
