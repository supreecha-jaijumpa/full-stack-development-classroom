# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (in progress) · 6.1 ✅ done |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 in progress — 6.1 ✅ done** |
| **Week Goal** | Work through Module 06 — 6.1 done; next is 6.2 Global State Management |
| **Last Session Date** | 2026-07-06 |
| **Last Session Summary** | **6.1 — Data Fetching (TanStack Query). Taught at peer/architect altitude + exercise passed 8/10.** Skipped `useQuery` syntax (student uses it at work); anchored the whole topic on the **one distinction that justifies the library: server state vs client state, with staleness as the deciding property.** Server data is remote + shared + cacheable → your copy goes **stale** the moment it lands because someone else can change the source; client data (a collapsed sidebar) can never go stale because only you change it → `useState`. **Articulation was the real work this session.** Student opened with **"I can't explain it"** — scaffolded hard: fill-in-the-blank sentence, one blank at a time, "clumsy-and-out-loud beats perfect-and-silent." Student got confused first (dropped the *subject* into the blanks), then built the full contrastive sentence themselves ("list of files lives on the **server**, can **change** without my app knowing → **stale**; sidebar lives in `useState`, only the user changes it → can't go stale"). Real jump from "can't explain" to a complete both-sides thought in their own words. Minor English polish noted (ser**v**er; "goes stale" not "go stale"). **Q2 (`staleTime` vs triggers):** taught the two-gate model — `status`/`staleTime` (do I have/trust data?) is a *separate axis* from `fetchStatus`/triggers (am I fetching now?); stale ≠ refetch-now; a refetch needs stale **AND** a trigger (mount/refocus/reconnect/invalidate) → that's stale-while-revalidate, the fix for last session's blocking-Cart. On the SWR articulation, student again went **vague** ("don't show loading too often, best experience") — pushed for **concrete nouns on the screen**: built version = customer stares at a **loader** 800ms for a cart the app already knew; correct = **products instantly** from snapshot, prices **change** in background, no spinner. Landed payoff on the customer. **Exercise (`/check` 8/10, Pass):** RepoList vs GitHub API — fetcher throws on `!res.ok` (forks 404), all four states as distinct branches incl. **empty** as first-class (redeeming the capstone miss), `['repos', username]` key, zero useEffect/useState-for-data. **One real bug flagged:** gated loading on `isPending`, which + `enabled` hangs the spinner forever (disabled query = `status:'pending'` + `fetchStatus:'idle'`); fix is `isLoading`. Plus a silent-truncation nit (`per_page=100`, first page only). |
| **Homework** | None assigned |
| **Homework Status** | — |
| **Next Action** | Continue Module 06 with **6.2 Global State Management**. Bridge from 6.1: the student already articulated *why the sidebar isn't server state* — 6.2 is how to manage that **client** state well when it outgrows `useState` (when to lift, when to reach for a store, selector subscriptions — which they used correctly in the capstone). **Keep pressing the articulation edge:** the pattern this session was vague-headline-first ("best experience") → coach to **concrete nouns / mechanism + payoff on a person.** Student responds well to fill-in-the-blank scaffolding when stuck. |
