# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (in progress) · 6.1 ✅, 6.2 ✅, 6.3 ✅, 6.4 ✅, 6.5 ✅ taught (exercises pending) |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 in progress — 6.1 ✅, 6.2 ✅, 6.3 ✅ (homework passed 2026-07-07), 6.4 ✅ taught 2026-07-16, 6.5 ✅ taught 2026-07-17 (6.4 & 6.5 exercises not yet done)** |
| **Week Goal** | 6.5 Component Design taught at architect altitude — next: do the 6.5 exercise (refactor a hard-coded component into one reusable prop-driven component used in ≥2 places), or the 6.4 exercise, or move to 6.6 |
| **Last Session Date** | 2026-07-17 |
| **Last Session Summary** | **6.5 — Component Design & Reusability. Architect altitude (student builds components daily → skipped mechanics; whole session = API-surface judgment + English articulation).** Opened with the altitude shift: a mid-level dev asks *"how do I build a component that does what this screen needs?"*, an architect asks *"what's the smallest surface I can expose so every screen — including the ones that don't exist yet — can use it, and none can misuse it?"* Anchor: **props are a public API / a published contract you promise to keep** — closing the module's through-line: **6.3 = contract for shape (Zod), 6.4 = contract for look (tokens), 6.5 = contract for behavior (props).** Four concepts. (1) **A wide API is a liability, not a feature** — props should express *intent* (`variant="primary"`), not *implementation* (`paddingX={16}`); every prop is a promise you can't take back, so a wide API doesn't just cause drift, it *freezes your own hands*. **Strong unaided Q1** — student named drift + inconsistency AND put a person in the payoff (the maintainer hunting every button to change one border-radius); sentence tightened to problem→consequence→who-pays. (2) **`children`/composition = the safe escape hatch** — hand out *slots for content*, never *decisions*; content is meant to differ (cheap dup), decisions must stay in one place (expensive). Q2 stuck → **multiple-choice** (picked A: children varies content, paddingX varies a design decision) → clean say-back. (3) **Controlled vs uncontrolled = who owns the state** — support both = **progressive disclosure**: cheap door (`defaultActive`) for the 90% caller, powerful door (`active`+`onActiveChange`) for the 10% who must coordinate the value from outside; neither taxed by the other. **Both halves correct unaided (Q3)**; sentence tightened on "change the value *from outside*." (4) **Don't extract too early** — coincidental similarity (looks alike today) ≠ essential similarity (changes together for the same reason); extract only on the latter; wrong abstraction costs more than duplication (magnet for props/`if` branches serving two masters) → **rule of three**. Q4 stuck → fill-in *also* stalled → **full multiple-choice** (playbook move again: picked B + E) → then **clean solo synthesis** of both halves in own words. **No exercise this session** — student ran `/done` after teaching; offered Exercise 6.5 (refactor a hard-coded component into one reusable, prop-driven component used in ≥2 places) for next time. **Articulation verdict:** noticeably stronger — Q1 and Q3 were unaided and correct; every say-back landed clean and well-shaped once the choice was in hand; the multiple-choice→say-it-back drill unblocked every stall. English typos persist (matainance/thruth/aprt) — content unaffected. |
| **Homework** | **Sign-up build — both sides of the trust boundary (~1–2 h, scoped from the 6.3 exercise):** `signup/schema.ts` — one Zod schema (`email`, `password`, `username`) + `type SignupForm = z.infer<...>`, **zero hand-written interfaces**, plus a required 3-sentence comment (why the same schema runs in two places and why that's not duplication — full sentences, concrete nouns). · `SignupForm.tsx` — RHF + `zodResolver`, uncontrolled `register`, inline errors wired with `aria-invalid` + `aria-describedby`, submit disabled while invalid or submitting. · `server.ts` — Express route (or simulated handler) calling `safeParse` on the **same imported schema**, returning 400 + flattened field errors; include one test or curl call proving `{email:"x",password:"a"}` dies at the gate. · Round trip: server-only "email already taken" error mapped back via `setError("email", { message })` into the same inline error spot. · Stretch (optional): `.strict()` + a comment on what it changes about the silent-drift bug. |
| **Homework Status** | ✅ Submitted & reviewed 2026-07-07 — **passed, 9.5/10** (`exercises/6.3-forms-and-validation/`). First submission was 8/10: architecture right, articulation comment excellent, but **the code had never run** (no package.json, no curl/test proof) + silent failure on network/non-JSON errors + a half-wrong `.strict()` comment example. Student fixed **all three** findings without hand-holding: added a real project (Zod v4, Express 5, vitest + supertest), a 4-test suite (curl-guy 400, strict-mode unknown-key rejection incl. a smuggled `isAdmin`, 409 "email taken" in the setError shape, happy-path 201), try/catch with `setError("root")` fallbacks for network + non-JSON failures, a `role="status"` success state, and corrected the comment's precision (renamed required field fails loudly; only *extra* keys are silent). Mentor verified: `vitest run` 4/4 green, `tsc --noEmit` clean. Micro-nits left (non-blocking): package.json `test` script is still the npm-init placeholder (wire `"test": "vitest run"`), package name is `general`. |
| **Next Action** | **Exercise 6.5 (or move to 6.6).** 6.5 was taught but not built — offer the refactor exercise: take a hard-coded, one-off component and turn it into **one reusable, prop-driven component used in ≥2 places**, where the student *designs the prop API themselves* — draws the intent-vs-implementation line, decides what becomes a prop vs `children`, and defends the surface as the smallest that can't be misused. Architect-relevant *API design under constraints*, and it exercises the written-review muscle the 6.3 homework showed the student handles cleanly. Two exercises now stand pending (6.4 themed-dashboard styling system AND 6.5 refactor) — can bundle or pick one. If they'd rather keep moving, next topic is **6.6** (check the module README for the next slot). **Articulation playbook (keeps working):** fill-in-the-blank on a stall → if that *also* stalls, drop to **full multiple-choice** (unblocked every stall in 6.3, 6.4, 6.5) → then always make the student *say the whole thing back in their own words*. Keep enforcing *people in the payoff* and clean sentence shape (problem→consequence→who-pays). Student's concepts are strong and increasingly unaided (Q1 and Q3 in 6.5 were clean solo answers) — the sole growth edge remains English articulation under pressure. |
