# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (in progress) · 6.1 ✅, 6.2 ✅, 6.3 ✅ done (homework passed) |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 in progress — 6.1 ✅, 6.2 ✅, 6.3 ✅ (homework passed 2026-07-07)** |
| **Week Goal** | 6.3 homework submitted & passed — next: 6.4 Styling Systems |
| **Last Session Date** | 2026-07-07 |
| **Last Session Summary** | **6.3 — Forms & Validation. Architect altitude (student uses RHF + Zod daily — wiring skipped); three judgment calls + the usual articulation work.** (1) **Controlled vs uncontrolled — a genuine knowledge gap surfaced.** Student had value-ownership *backwards* (said controlled value lives in "RHF form values", uncontrolled in "parent state") and reached for `Controller` as RHF's core trick. Taught the ownership model: **controlled = React state owns the value** (input is a puppet; every keystroke → setState → re-render — student correctly traced the 12-field controlled form through 6.2's two re-render causes: own-state for the form, parent-re-render for the other 11 fields); **uncontrolled = the DOM node owns the value** (native browser behavior), React reads via ref → zero re-renders. RHF `register` = uncontrolled + mutable internal ref + **subscription** re-renders (`formState`, `watch`) — bridged explicitly as *"RHF is a form-scoped store"*, the same slice-subscription architecture as 6.2's Zustand; `Controller` = escape hatch for must-be-controlled third-party inputs. Articulation: student hit **"no idea"** on the user-visible symptom → mechanism timeline + **multiple-choice scaffold** (new move — use when fill-in-the-blank also stalls; it worked) → picked correctly: *typed letters trail your fingers and catch up in a burst when you pause*; mentor modeled the full architect sentence. (2) **Validation as a trust boundary** (callback to 1.3). curl skips the entire React app — client Zod executes on the attacker's machine ("the client is enemy territory"). Two **English-structure** fixes: the payoff blanks need *people*, not things (student answered "invalid values, payload"), and **"protect A from B" word order** (student wrote "protects hacker from database" — A = the thing kept safe, comes first). Landed sentences: *client validation protects the honest user from frustration* (typo → instant red field vs an 800ms round-trip) — a courtesy; *server validation protects the database from every client on the internet* — the law. Student closed clean: client is optional (lose UX), **missing server validation = security incident** (curl lands `email: "a"` as a row in users). (3) **Schema as single source of truth.** On interface↔schema drift the student predicted "may error" → corrected to the scarier truth: **nothing errors** — TS never compares the two declarations (build passes) and `z.object` **strips unknown keys by default** (validation succeeds; the new field is silently deleted) → silent data loss, discovered as nulls weeks later. **Type erasure was brand-new** ("no idea" → taught): an interface compiles to zero bytes of JS; the Zod schema survives as a runtime value; `z.infer` reads the type off it at compile time; rule locked in: **derivation flows value → type, never type → value**. Student rebuilt every target sentence (fragmentary but correct). **In-session exercise skipped at student's request → converted to this week's homework.** Two entries logged to `corrections.md`. Minor English typos persist (declration/unknow) — content unaffected. |
| **Homework** | **Sign-up build — both sides of the trust boundary (~1–2 h, scoped from the 6.3 exercise):** `signup/schema.ts` — one Zod schema (`email`, `password`, `username`) + `type SignupForm = z.infer<...>`, **zero hand-written interfaces**, plus a required 3-sentence comment (why the same schema runs in two places and why that's not duplication — full sentences, concrete nouns). · `SignupForm.tsx` — RHF + `zodResolver`, uncontrolled `register`, inline errors wired with `aria-invalid` + `aria-describedby`, submit disabled while invalid or submitting. · `server.ts` — Express route (or simulated handler) calling `safeParse` on the **same imported schema**, returning 400 + flattened field errors; include one test or curl call proving `{email:"x",password:"a"}` dies at the gate. · Round trip: server-only "email already taken" error mapped back via `setError("email", { message })` into the same inline error spot. · Stretch (optional): `.strict()` + a comment on what it changes about the silent-drift bug. |
| **Homework Status** | ✅ Submitted & reviewed 2026-07-07 — **passed, 9.5/10** (`exercises/6.3-forms-and-validation/`). First submission was 8/10: architecture right, articulation comment excellent, but **the code had never run** (no package.json, no curl/test proof) + silent failure on network/non-JSON errors + a half-wrong `.strict()` comment example. Student fixed **all three** findings without hand-holding: added a real project (Zod v4, Express 5, vitest + supertest), a 4-test suite (curl-guy 400, strict-mode unknown-key rejection incl. a smuggled `isAdmin`, 409 "email taken" in the setError shape, happy-path 201), try/catch with `setError("root")` fallbacks for network + non-JSON failures, a `role="status"` success state, and corrected the comment's precision (renamed required field fails loudly; only *extra* keys are silent). Mentor verified: `vitest run` 4/4 green, `tsc --noEmit` clean. Micro-nits left (non-blocking): package.json `test` script is still the npm-init placeholder (wire `"test": "vitest run"`), package name is `general`. |
| **Next Action** | **6.4 — Styling Systems.** Student uses Tailwind daily → architect altitude: design tokens as a contract, the utility-vs-component-abstraction line, variants (cva), theming (ties back to 6.2's ThemeContext + 2.2's custom-property architecture). **Articulation playbook:** fill-in-the-blank on a stall; if that *also* stalls, drop to **multiple-choice** (worked in 6.3); keep enforcing *people in the payoff* and English structure patterns ("protect A from B" — A is the thing kept safe). Note the homework round-trip showed the student can act on a written review cleanly — consider more written-review-style exercises (architect-relevant). |
