# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 07 — Frontend Testing & Quality · **2/6 topics taught (7.1 ✅, 7.2 ✅)** |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); Module 06 ✅ (7/7 + milestone, 2026-07-24). **Module 07 started 2026-09-14 after a ~7-week break — 7.1 Testing Fundamentals and 7.2 Unit Testing (Vitest) both taught that day. Next: 7.3 Component Testing (React Testing Library).** |
| **Week Goal** | Get the **7.2 hands-on** in as real code (the student has *understood* unit testing but has not yet *run* a test — 7.2's exit bar is "passing tests, run in watch mode while refactoring"), then teach **7.3 Component Testing**. Collect the 7.1 test plan before 7.4. Same cadence: architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last question pushed for the who-pays ending. |
| **Last Session Date** | 2026-09-14 |
| **Last Session Summary** | **7.2 Unit Testing (Vitest) — taught in two parts, mechanics included** (module is genuine new ground, so syntax was not skipped). All examples grounded in Repo Pulse. **Part 1:** why Vitest (reuses Vite's transform pipeline → tests compile what ships; Jest in Vite = a parallel config that drifts); setup (`vitest` watch vs `vitest run` for CI, colocated `*.test.ts`, explicit imports over `globals`); anatomy (`describe`/`it`/`expect`, names that read as promises); matcher table + traps (`toBe` on objects, `expect(fn()).toThrow()`); **`it.each` edge-case tables** with a real bug found by running it — a `formatCount` that passes `42`/`1500` but returns **`"1000k"` for 999,950** (threshold checked before `toFixed` rounds across it); where edge cases hide (zero, boundaries, rounding/carry, invalid input); a Zod schema test that **pins the no-`.strict()` architecture decision**; watch mode + `.only` failing CI. **Part 2:** test isolation (`beforeEach` builds, `afterEach` undoes globals); `vi.fn()` injected via `fetchImpl = fetch` default param — framed as dependency injection / the D in SOLID at function scale; `vi.mock` for directly-imported modules (hoisting, factory can't see file variables → `vi.hoisted`, `vi.mocked` cast), with the warning that it's the easy road to mocking your own internals; fake timers for "pushed 3 days ago"; reading coverage (uncovered line #s first, branch > line, 100% ≠ correct); **Goodhart's law** + alternatives (review signal, ratchet, regression test per bug fix). **Guiding question** (why happy-path tests missed the bug + why coverage can't catch it): first reply described only the mechanism ("the function rounds up") → fill-in-the-blank with options → B, B correct → own-words restatement **copied option B verbatim**; honest note given, blunter model version shown, not re-drilled. **Say-back Q1** (clock-dependent test with no fake timers) — **pass unaided**, even volunteered a who-pays ("next developer must update the date"); sharpened: the fixed input isn't the problem, the moving real "now" is. The one-time repeat of the tightened version was skipped. **Q2** (tech lead's 90% coverage CI gate) — first draft was only the problem slot, recycled from the mentor's Part 1 line → MC on consequence + who-pays (B, C correct) → **full three-beat answer in the student's own words** ("just raise coverage number", "maintains got harder") — **first genuinely re-worded restatement, the watch-item from 7.1 moved** — but who-pays was half: maintenance cost only, no named payer and no users/false-confidence half → coached → final repeat **included both halves (team + users) in own words. Pass.** English: subject–verb agreement (*team pay → pays*), spelling (maintainance, unnecessery, develper, becauze, timestmap, confortable). **Articulation verdict:** real progress on own-phrasing when *composing* a full answer; still copies option text when the scaffold hands over sentence-shaped choices, and who-pays tends to come back half-complete (cost without payer, or one payer of two). |
| **Previous Session Summary** | **7.1 Testing Fundamentals — taught from zero** (student self-rated A = almost no testing experience; examples grounded in Repo Pulse). Frame: manual testing proves the app worked once; a test re-runs that check on every change. Four concepts: pyramid/trophy = confidence vs cost (**push each test to the lowest level that can still catch the bug**); behavior not implementation (refactor litmus, false alarms teach a team to ignore red); test doubles (stub/fake vs mock split by *what you assert*; fake boundaries you don't own, don't mock your own internals); what's worth testing (logic, trust boundaries, error states, critical flows, regression per bug; not coverage-as-target). Q1 half right (chose integration for a pure formatter → unit; extract logic to make it unit-testable). Q2 unblocked by multiple-choice → first single-pass who-pays ending. |
| **Homework** | **(1) 7.2 hands-on — real code, ~45 min, in Repo Pulse, pasted into the conversation for review:** add Vitest; write the `formatCount` `it.each` boundary table and watch it go red at 999,950; **fix the bug with watch mode running** (hint given only: *when* rounding happens vs *when* the unit is chosen — do not hand over the fix); test the repo fetcher for success / 404 / 500 with a `vi.fn()` stub — if the fetcher can't take an injected `fetch`, that's a design conversation to have. **(2) Still open from 7.1:** written Repo Pulse test plan — classify each feature unit / component / E2E with a one-line reason (~30 min). |
| **Homework Status** | Both not started — offered at end of 7.2; student ran `/done` |
| **Next Action** | **If homework is submitted → review the real code first** (run `vitest run`, confirm the 999,950 row is green and the fix doesn't just special-case that number — e.g. check 999,949 / 1,000,000 / 1,050 still correct; check fetcher tests assert outcomes, not calls). **Otherwise → teach 7.3 Component Testing (React Testing Library):** `jsdom` environment, `render`/`screen`, query priority (`getByRole` first — ties to 2.5 accessibility tree), `userEvent` over `fireEvent`, `findBy*` for async, and faking the network at the boundary (MSW) rather than mocking `useRepoStats` — the natural subject is Repo Pulse's "User not found" / loading / empty states. Cadence unchanged: architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last question pushed for who-pays. **Articulation playbook:** fill-in-the-blank → multiple-choice if stalled → say the whole thing back. **Watch-items:** (a) when MC options are sentence-shaped the student copies them verbatim — make options **short labels/keywords** so the sentence must be composed; (b) who-pays comes back half-complete — prompt "name *who* and *how many* payers" explicitly; (c) the one-time repeat of a tightened version is getting skipped — keep it to a ≤10-word version so it's cheap. Module 07 milestone = Repo Pulse at production quality (unit + component + one E2E flow + a11y audit + Lighthouse pass + written testing strategy). |
