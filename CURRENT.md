# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (in progress) · 6.1 ✅, 6.2 ✅ done |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 in progress — 6.1 ✅, 6.2 ✅ done** |
| **Week Goal** | Work through Module 06 — 6.1 & 6.2 done; next is 6.3 Forms & Validation |
| **Last Session Date** | 2026-07-06 |
| **Last Session Summary** | **6.2 — Global State Management. Taught at architect altitude (skipped `createContext`/Zustand syntax — student uses both at work); whole session on *judgment* + *articulation*.** Anchored on **three moves**: (1) **the state-placement ladder** — local `useState` → lift+props → global; global is the *last* rung and the word that justifies climbing is **prop drilling** (value must reach components *far apart*, threading through layers that never read it). Student jumped straight to "global" and skipped the lift rung; corrected. (2) **Context vs store** — Context when the value changes **rarely** (theme, auth); store when it changes **frequently AND has many independent readers**. Drew out two *different* store-reasons: cursor→store for **speed** (60/sec), cart→store for **selectivity** (badge reads `count`, drawer reads `items` → slice subscription re-renders only what changed). Student sorted all four scenarios (auth/theme→Context, cursor/cart→store) correctly. (3) **The re-render trap** — a component re-renders from *own state/props* OR *parent re-rendering*; student initially concluded `useMemo` on a Context value is useless ("theme change re-renders all anyway"), hit **"no idea"** on the real reason → taught the two causes, then student rebuilt the full sentence: *"useMemo matters because ThemeProvider re-renders whenever the parent re-renders, and without it that rebuilds the value object, making every consumer re-render even though content didn't change."* Logged to `corrections.md`. **Exercise (write it):** production-grade typed `ThemeContext` — `null` default + throwing `useTheme()` (clean non-nullable type), `useCallback`-stable `toggleTheme` **connected to** the `useMemo`-pinned value (nailed that both must be stable or neither works). No score given (discussion-led). **Articulation pattern held from 6.1:** student defaulted to vague headlines ("make performance better", "subscribed component re-renders") → coached repeatedly to **concrete nouns on a real screen** (YouTube header: "the avatar stops re-rendering when a notification arrives"). Structure = headline → template → concrete instance; student produces the template cleanly, still needs a push to drop in real nouns. Fill-in-the-blank scaffolding worked again on "no idea". Minor English typos throughout (subscribe/slice/instead/without). |
| **Homework** | None assigned |
| **Homework Status** | — |
| **Next Action** | Continue Module 06 with **6.3 Forms & Validation** (React Hook Form + Zod). Student already uses RHF + Zod at work → teach at architect altitude, skip basic wiring. Likely angles: **validation as a trust-boundary decision** (client = UX, server = integrity — callback to 1.3), schema as single source of truth (infer types from Zod, don't hand-write twice), controlled-vs-uncontrolled + why RHF minimizes re-renders (ties directly to 6.2's re-render mechanics — RHF keeps inputs *uncontrolled* to avoid re-rendering the whole form on every keystroke). **Keep pressing the articulation edge:** same pattern — student gives the correct *template* sentence but stays abstract; push for **concrete nouns / mechanism + payoff on a person**, and use fill-in-the-blank scaffolding the moment they stall ("no idea" → scaffold, don't lecture). |
