# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (in progress) · 6.1 ✅, 6.2 ✅, 6.3 ✅, 6.4 ✅ taught (exercise pending) |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 in progress — 6.1 ✅, 6.2 ✅, 6.3 ✅ (homework passed 2026-07-07), 6.4 ✅ taught 2026-07-16 (exercise not yet done)** |
| **Week Goal** | 6.4 Styling Systems taught at architect altitude — next: do the 6.4 exercise (themed dashboard styling system) or move to 6.5 Component Design |
| **Last Session Date** | 2026-07-16 |
| **Last Session Summary** | **6.4 — Styling Systems. Architect altitude (student writes Tailwind daily → skipped mechanics; whole session = system-design judgment + English articulation).** Opened with the altitude shift: a junior asks *"how do I make this look right?"*, an architect asks *"how do I make it impossible for the next engineer to make this look wrong?"* Four concepts, all one idea — **make valid choices declarable in one place, let tooling/cascade enforce them.** (1) **Design tokens as a contract** — the Tailwind config is the single source of truth for *look* (same architectural role as 6.3's Zod schema for *shape*); without it the design drifts into "40 shades of blue." Nailed the trade after coaching: **"tokens cost the engineer the freedom to type any hex, and buy the product visual consistency"** — landed *the constraint is the feature*. Articulation: Q1 content right but sentence tangled ("make we hard to matainance") → modeled the problem→consequence→guarantee shape; Q2 used **multiple-choice scaffold** (threat = "each engineer inventing values → drift", not "unknown values") + fill-in-the-blank for who-pays. (2) **Utility-vs-component line** — *"duplication of code is cheap; duplication of decisions is expensive."* Student gave an **excellent unaided Q3** (mb-4 on sections = code dup, safe to drift; CTA button = decision, must change all at once) — concept fully owned. The one-line test: *"if this changed, do I want all copies to change together or independently?"* (3) **The `<Stack>` premature-abstraction trap** — Q4 hit **"no idea"** → fill-in-the-blank *also* stalled → dropped to **full multiple-choice** (playbook move, worked again): student picked all 5 correct (rebuilt-flexbox / all-25 / change / one-shared-decision / look-alike), then synthesized the whole coupling argument in own words. (4) **cva** = variants as a typed table (typo = compile error); payoff framed on the *person* adding `intent="warning"` later — isolation + compiler safety. (5) **Theming** = semantic role tokens (`bg-bg`) → CSS custom properties; **student closed the 2.2 → 6.2 → 6.4 theming loop themselves** (custom props = swap point, ThemeContext = decides, tokens = components stay theme-blind) and answered Q6 cleanly (token way scales to N themes because components name a role, not a color). **No exercise this session** — student ran `/done` after the teaching; offered Exercise 6.4 (themed dashboard styling system) for next time. **Articulation verdict:** concepts consistently correct and often sharp (Q3 was unaided and strong); the *only* gap is pulling words out cold under pressure — the multiple-choice→say-it-back drill unblocked every stall. English typos persist (writen/matainance/inent/difference-for-different) — content unaffected. |
| **Homework** | **Sign-up build — both sides of the trust boundary (~1–2 h, scoped from the 6.3 exercise):** `signup/schema.ts` — one Zod schema (`email`, `password`, `username`) + `type SignupForm = z.infer<...>`, **zero hand-written interfaces**, plus a required 3-sentence comment (why the same schema runs in two places and why that's not duplication — full sentences, concrete nouns). · `SignupForm.tsx` — RHF + `zodResolver`, uncontrolled `register`, inline errors wired with `aria-invalid` + `aria-describedby`, submit disabled while invalid or submitting. · `server.ts` — Express route (or simulated handler) calling `safeParse` on the **same imported schema**, returning 400 + flattened field errors; include one test or curl call proving `{email:"x",password:"a"}` dies at the gate. · Round trip: server-only "email already taken" error mapped back via `setError("email", { message })` into the same inline error spot. · Stretch (optional): `.strict()` + a comment on what it changes about the silent-drift bug. |
| **Homework Status** | ✅ Submitted & reviewed 2026-07-07 — **passed, 9.5/10** (`exercises/6.3-forms-and-validation/`). First submission was 8/10: architecture right, articulation comment excellent, but **the code had never run** (no package.json, no curl/test proof) + silent failure on network/non-JSON errors + a half-wrong `.strict()` comment example. Student fixed **all three** findings without hand-holding: added a real project (Zod v4, Express 5, vitest + supertest), a 4-test suite (curl-guy 400, strict-mode unknown-key rejection incl. a smuggled `isAdmin`, 409 "email taken" in the setError shape, happy-path 201), try/catch with `setError("root")` fallbacks for network + non-JSON failures, a `role="status"` success state, and corrected the comment's precision (renamed required field fails loudly; only *extra* keys are silent). Mentor verified: `vitest run` 4/4 green, `tsc --noEmit` clean. Micro-nits left (non-blocking): package.json `test` script is still the npm-init placeholder (wire `"test": "vitest run"`), package name is `general`. |
| **Next Action** | **Exercise 6.4 (or move to 6.5).** 6.4 was taught but not built — offer the themed-dashboard styling-system exercise: student *designs the tokens*, draws the utility-vs-component line themselves, builds one cva component, and proves theming works by flipping one `data-theme` attribute (zero component edits). This is architect-relevant *system design under constraints*, and it exercises the written-review muscle the 6.3 homework showed the student handles cleanly. If they'd rather keep moving, next topic is **6.5 — Component Design & Reusability** (props as a public API — ties straight to 5.2's prop-contract work). **Articulation playbook (keeps working):** fill-in-the-blank on a stall → if that *also* stalls, drop to **full multiple-choice** (unblocked every stall in 6.3 and 6.4) → then always make the student *say the whole thing back in their own words*. Keep enforcing *people in the payoff* and clean sentence shape (problem→consequence→guarantee). Student's concepts are strong and increasingly unaided (Q3 in 6.4 was a clean solo answer) — the sole growth edge remains English articulation under pressure. |
