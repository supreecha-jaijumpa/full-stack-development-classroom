# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling · **all 7 topics taught** (6.1–6.7 ✅) · milestone pending |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); **Module 06 — 6.1 ✅, 6.2 ✅, 6.3 ✅ (homework passed 2026-07-07), 6.4 ✅ 2026-07-16, 6.5 ✅ 2026-07-17, 6.6 ✅ 2026-07-17, 6.7 ✅ 2026-07-23. All topics taught — the only remaining gate to Module 07 is the module milestone (data-driven dashboard). Mechanical build exercises dropped by student request 2026-07-17 — concept say-back + weekly homework are the checkpoints now** |
| **Week Goal** | Run the **Module 06 milestone** — a data-driven dashboard pulling the whole module together: TanStack Query data layer, Zod-validated boundary, token-driven Tailwind, reusable components with real prop contracts, a custom hook drawn at an actual seam, and at least one lazy-loaded route. Student submits real code; mentor reviews it like a PR before Module 07 unlocks. **Cadence changed 2026-07-23 (student found the say-back grind too slow): goal stays Software Architect, but the drill is lighter — 2 say-back questions (not 3), draft-then-polish (student answers messy → mentor tightens → student repeats once), and mentor gives the problem→consequence→who-pays skeleton up front instead of making him find it. Only the last question is pushed for the who-pays ending.** No build drills unless student asks; weekly homework stays as the real-code checkpoint |
| **Last Session Date** | 2026-07-23 |
| **Last Session Summary** | **6.7 — Frontend Performance (memo, lazy, code-split). Architect altitude (student optimises React at work → syntax skipped; whole session = *when optimising is worth it* + English articulation). Module 06 teaching now complete.** Frame: **React's default is fast; optimisation is a cost, not a free win.** Four concepts. (1) **Measure first** — a re-render is a function call + diff, *not* a repaint; every memo primitive costs memory + comparison work, and `useMemo`/`useCallback` add a **new correctness surface** (the deps array) — a bug class you volunteered for. Profile → fix the one proven hot path → leave the rest simple. (2) **`memo` + `useCallback`/`useMemo` are one system: referential stability** — memo compares props by reference, so an inline `onClick={() => …}`, `style={{}}` or `data={filter()}` **defeats it**, and a defeated memo is pure overhead; the architectural consequence = *you cannot memoise a component in isolation* — memoising the child obliges stabilisation on everything upstream (a coupling you bought). (3) **The best re-render fix is structural, not memo** — colocate state (push it down so fewer components sit under the change) and lift the heavy child up to pass as `children` (created in the grandparent's render, so the parent's own state change doesn't touch it); memo treats the symptom, moving state treats the disease. (4) **Code splitting is a different axis** — `React.lazy` + `<Suspense>` targets bundle size / time-to-interactive, not render speed; split where the user won't feel the gap (routes, modals, below-the-fold) and the code is genuinely heavy (charts, editors, map SDKs); two cliffs = too granular → request waterfall, too coarse → nothing gained. Also: pagination/virtualization beats memoising 10,000 rows. **Q1 (objection to a memo-everything PR):** solid unaided on both halves (per-use cost → only where profiling proves it; deps arrays = stale-state risk). **One precision correction: `React.memo` has no dependency array** — that's `useMemo`/`useCallback`; memo's cost is comparison + coupling, theirs is correctness. Who-pays ending missing again → modelled it (next dev inherits forty deps arrays he didn't write, debugs a UI showing yesterday's data, no profiling evidence anywhere). **Q2 (memo'd child still re-renders):** clean unaided — inline object/function props without `useMemo`/`useCallback`. **Q3 (two axes + the tell-tale symptom):** "stuck" → fill-in-the-blank also stalled → **full multiple-choice** (A1/B2/C2 — all correct) → **clean solo say-back of both axes in his own words**. One tense fix: a slow first paint that goes snappy afterwards is the *signal to split*, not the *result of splitting*. **Articulation verdict:** unchanged pattern — concepts land unaided (Q1, Q2), the stall is always the **consequence / who-pays ending**, and fill-in → multiple-choice → say-it-back unblocked it again. Note: this session still ran the *old* 3-question cadence; the lighter 2-question / skeleton-up-front cadence logged 2026-07-23 applies from the next topic on. English typos persist (perfomance/depadencies/buddle/spliting) — content unaffected. |
| **Previous Session Summary** | **6.6 — Custom Hooks. Architect altitude (student uses hooks daily → skipped mechanics; whole session = seam/contract judgment + English articulation).** Opened with the altitude shift: a mid-level dev extracts a hook to *kill duplication* (trigger = repetition); an architect extracts one to *draw a seam between behavior and presentation* (trigger = "what mechanism do I want to hide behind a stable interface so components stay dumb and the implementation stays swappable?"). Anchored to the module through-line: **6.3 = contract for shape (Zod), 6.4 = contract for look (tokens), 6.5 = contract for behavior *inside one component* (props), 6.6 = contract for behavior *portable across components* (the hook's return value).** Four concepts. (1) **A hook is a seam between behavior and presentation** — `useUser(id)` behind `ProfileCard` lets the data layer (fetch→TanStack Query, REST→GraphQL) be swapped with zero component edits; payoff = the component and mechanism change *independently*. **Strong unaided Q1** (both altitudes + the swap payoff); coached to put a *person* in the payoff (the next dev edits one hook, not forty screens) and tighten sentence shape (problem→consequence→who-pays). (2) **Return value is a public API** — object over positional array (arrays are position-locked → can't grow the contract without breaking callers; objects are additive); expose intent, not internals. **Clean unaided Q2** (both halves: why object + the exactly-two-things tuple exception); added the missing consequence (position-locked = can't evolve the API). (3) **Hooks compose** — HOCs/mixins/inheritance all solved "how do two components share behavior"; hooks solve it with **composition** (`useAuth` = `useContext` + `useQuery`); rule: *one hook, one responsibility* (three jobs = three reasons to change). Q3(a) got the problem but missed the one-word name **composition**; Q3(b) stalled at "violates SRP" → fill-in-the-blank (three/other/one — correct) → then coached the who-pays ending (the dev who wanted only the fetch is forced to drag the form + query-param sync too). (4) **Extract on essential, not coincidental sameness** (the 6.5 trap again) — `useDebounce`/`useUnsavedChangesGuard` = essential (same behavior everywhere), fake `useForm` over user-form + checkout-form = coincidental (share variable names, evolve separately); rule of three; wrong shared hook > duplication because callers depend on its *shape*. **Excellent unaided first half of Q4** (drew the essential/coincidental line himself with no help); second half (why early extraction is worse) stalled → fill-in stalled → **full multiple-choice** (A/B/A — correct) → said it back. **Articulation verdict:** noticeably stronger — Q1 and Q2 were clean unaided, and the essential-vs-coincidental call in Q4 was genuinely sharp with no scaffold. The only stalls were the *consequence / who-pays* endings (Q3b, Q4), which the fill-in → multiple-choice → say-it-back playbook unblocked every time. English typos persist (setween/compy/becuse/eactly) — content unaffected. |
| **Homework** | **Sign-up build — both sides of the trust boundary (~1–2 h, scoped from the 6.3 exercise):** `signup/schema.ts` — one Zod schema (`email`, `password`, `username`) + `type SignupForm = z.infer<...>`, **zero hand-written interfaces**, plus a required 3-sentence comment (why the same schema runs in two places and why that's not duplication — full sentences, concrete nouns). · `SignupForm.tsx` — RHF + `zodResolver`, uncontrolled `register`, inline errors wired with `aria-invalid` + `aria-describedby`, submit disabled while invalid or submitting. · `server.ts` — Express route (or simulated handler) calling `safeParse` on the **same imported schema**, returning 400 + flattened field errors; include one test or curl call proving `{email:"x",password:"a"}` dies at the gate. · Round trip: server-only "email already taken" error mapped back via `setError("email", { message })` into the same inline error spot. · Stretch (optional): `.strict()` + a comment on what it changes about the silent-drift bug. |
| **Homework Status** | ✅ Submitted & reviewed 2026-07-07 — **passed, 9.5/10** (`exercises/6.3-forms-and-validation/`). First submission was 8/10: architecture right, articulation comment excellent, but **the code had never run** (no package.json, no curl/test proof) + silent failure on network/non-JSON errors + a half-wrong `.strict()` comment example. Student fixed **all three** findings without hand-holding: added a real project (Zod v4, Express 5, vitest + supertest), a 4-test suite (curl-guy 400, strict-mode unknown-key rejection incl. a smuggled `isAdmin`, 409 "email taken" in the setError shape, happy-path 201), try/catch with `setError("root")` fallbacks for network + non-JSON failures, a `role="status"` success state, and corrected the comment's precision (renamed required field fails loudly; only *extra* keys are silent). Mentor verified: `vitest run` 4/4 green, `tsc --noEmit` clean. Micro-nits left (non-blocking): package.json `test` script is still the npm-init placeholder (wire `"test": "vitest run"`), package name is `general`. |
| **Next Action** | **Run the Module 06 milestone — the data-driven dashboard.** All 7 topics are taught; this is the single remaining gate to Module 07. Scope it to ~1–2 h with an explicit rubric and have the student submit **real code** for a PR-style review. It must exercise the module's through-line, one item per topic: TanStack Query as the server-state layer (6.1), a deliberate client-vs-server state split (6.2), a Zod-validated boundary with types via `z.infer` (6.3), token-driven Tailwind with no ad-hoc hex values (6.4), at least one component with a narrow intent-based prop contract (6.5), one custom hook drawn at a real seam — not merely to kill duplication (6.6), and at least one lazy-loaded route with a real fallback (6.7). Build exercises are off by student request (2026-07-17) — student builds React daily, so mechanical drills waste time; the concept say-back and the weekly homework carry the practice load. **Cadence per topic (revised 2026-07-23):** teach at architect altitude → **2** say-it-back questions → move on. **Draft-then-polish:** student answers in one messy pass, mentor hands back the tightened sentence, student repeats it once — do NOT make him grind unaided for wording. **Give the skeleton up front:** "X happens → so Y breaks → and Z eats it"; he fills slots rather than rediscovering the form. Only the **last** question is pushed for the who-pays ending; concept-only passes on the first. Do NOT offer or nag build exercises unless the student asks. **Articulation playbook (still the fallback on a stall):** fill-in-the-blank → if that *also* stalls, **full multiple-choice** (unblocked every stall in 6.3–6.6) → then say the whole thing back in his own words. The recurring growth edge is specifically the **consequence / who-pays ending** of a point — the student lands the concept and mechanism unaided but stalls on "so who suffers?"; keep drilling *people in the payoff* + sentence shape (problem→consequence→who-pays). Concepts are strong and increasingly unaided. Weekly homework stays as the one real-code checkpoint before the module unlocks. |
