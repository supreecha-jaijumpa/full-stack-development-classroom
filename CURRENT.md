# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 06 — Frontend Data, State & Styling (next) · Module 05 complete ✅ |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); **Module 05 ✅ complete (7/7)** — 5.1–5.6 + 5.7 capstone |
| **Week Goal** | Kick off Module 06 — start with 6.1 Data Fetching (TanStack Query) |
| **Last Session Date** | 2026-07-06 |
| **Last Session Summary** | **5.7 — Build a Multi-Page SPA (Module 05 capstone). Module 05 complete.** Student built a product-catalog SPA solo (list + detail + cart across 3 routes) and submitted **real code** for review — passed the capstone and went past spec. Ran a pre-build plan at architect altitude (3 questions). **Q2 (where does cart state live) was the meaty one:** student picked Zustand but justified it with "persist," which I corrected — Zustand is in-memory too (dies on refresh without `persist` middleware), and a lifted `useState` *does* survive navigation, so "persist" isn't the real differentiator. The architect reason is **consumer location**: the cart is read/written from scattered places (navbar badge, list add-buttons, cart page), so lifting + prop-drilling couples everything to `App`; a store lets each subscribe directly, and with a **selector** the badge re-renders only when *count* changes. Student re-said it correctly. **Q3:** gave loading/error/data, **missed `empty`** — coached that a successful fetch returning `[]` is a distinct happy-path state (the one the spec names). **Code review of real code:** passes every success criterion (3 routes, shell rendered once via `<Outlet>`, deep-link `/products/:id` via `useParams`, all four list states, selector subscriptions genuinely used). Unprompted senior move: the Cart treats the persisted store as a **cache of `{id, quantity}`** and re-joins against freshly-fetched products, with a "product no longer sold" branch — shows they grasp that a persisted cart is *stale data*. **Three critiques handed over:** (1) right instinct (don't trust persisted prices) but wrong mechanism — a **blocking** refetch shows an 800ms spinner every visit for data it already has a snapshot of; the correct shape is **stale-while-revalidate** (render from snapshot, revalidate in background) = what TanStack Query does. (2) The real smell: the `fetch + status + cleanup + Status`-type block is copy-pasted **3×** (List/Detail/Cart) — *this is the boilerplate server-state libraries delete*; ideal bridge into Module 06. (3) Minor: header count (all quantities, incl. unavailable) disagrees with total (available only) — pick one definition of "what's in the cart." **Articulation (growth edge):** applied last session's rule (payoff = user/business value, not code). Student's first 3-beat on the cache decision was still code-framed ("system won't get a stale-price bug") *and* mis-described their own code ("cache only IDs" when they actually persist the full snapshot). Coached both: state the truer, more sophisticated claim (persist the snapshot, trust only `id`+`quantity`, re-fetch the rest) and land the payoff on a **person** (the customer never gets surprise-charged at checkout — the number in the cart is the number they pay). Modeled the full 3-beat. |
| **Homework** | None assigned |
| **Homework Status** | — |
| **Next Action** | Start **Module 06 — Frontend Data, State & Styling** with **6.1 Data Fetching (TanStack Query)**. Open by cashing in the capstone's own pain: the 3× hand-rolled `fetch + loading/error/empty + cleanup` block IS the boilerplate `useQuery` deletes, and the Cart's blocking refetch is exactly the case for stale-while-revalidate. Frame 6.1 as "the tool you just proved you needed." |
