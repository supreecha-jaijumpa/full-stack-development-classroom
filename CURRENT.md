# Current Session Snapshot
> Updated by mentor after every session. Read this at session start instead of `progress/log.md` + `plan/roadmap.md`.

| Field | Value |
|-------|-------|
| **Profile** | Senior frontend dev (6 yrs, React/Next) → goal: Software Architect |
| **Current Phase** | Phase 2 — Frontend Development |
| **Current Module** | Module 07 — Frontend Testing & Quality · **3/6 topics taught (7.1 ✅, 7.2 ✅, 7.3 ✅)** |
| **Status** | Module 02 ✅ (6/6); Module 03 ✅ (8/8); Module 04 ✅ (6/6); Module 05 ✅ (7/7); Module 06 ✅ (7/7 + milestone, 2026-07-24). **Module 07 started 2026-09-14 after a ~7-week break — 7.1 + 7.2 taught 2026-09-14, 7.3 Component Testing (RTL) taught 2026-09-14 → 09-15. Next: 7.4 E2E Testing (Playwright).** |
| **Week Goal** | Teach **7.4 E2E (Playwright)**, then **7.5 Accessibility Testing**. The student has still never *run* a test (7.1/7.2 homework skipped by request; 7.3 practice was a written, unrun worked example) — the Module 07 milestone is the first real, executed test code, so review it closely there. Same cadence: architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last question pushed for the who-pays ending. |
| **Last Session Date** | 2026-09-15 |
| **Last Session Summary** | **7.3 Component Testing (React Testing Library) — taught in two parts, mechanics included.** Session opened with the student asking to **skip all pending homework** (7.1 test plan + 7.2 hands-on) → recorded as skipped. **Part 1** (grounded in a Repo Pulse `UserSearch` form): frame = test only the surfaces a user or parent can touch (props + user actions in; DOM + callback-prop calls out), RTL gives no access to state; setup (`jsdom`, jest-dom via `@testing-library/jest-dom/vitest`, manual `afterEach(cleanup)` because `globals` is off); query variants (`getBy` now / `queryBy` absence only / `findBy` will appear) + priority (`getByRole` first — **a failed `getByRole` is a free a11y check**); `userEvent` over `fireEvent`; jest-dom matchers; asserting a callback prop = behavior, asserting a setter/hook = internals. **Guiding question** (`getByTestId` + `<button>`→`<div onClick>` refactor): 1 ✅ test stays green, 2 ✅ keyboard + screen-reader users locked out, 3 ⚠️ vague ("keep app accessibility") — named a goal instead of the mechanism (`getByRole` throws because the div has no button role). **Part 2** (Repo Pulse dashboard states): `findBy*` / `waitFor` / `waitForElementToBeRemoved` + `waitFor` traps; **where to fake** — `vi.mock` the data hook skips own Zod/Query/fetch vs **MSW at the network**; MSW v2 setup (`onUnhandledRequest: 'error'`, `resetHandlers`, per-test `server.use`), fixtures parsed through the real schema; fresh `QueryClient` per test with `retry: false` + the edge case that **per-query `retry` overrides client defaults** (5xx test blows past `findBy`'s 1 s); four state tests incl. a corrupted-payload test that automates the milestone's manual toggle. **Student then asked for "more example"** → mentor read a real **Role & Permission admin feature from the student's work codebase** (Next 16 + antd 6 + next-intl + axios + RHF/Zod + TanStack Query) and wrote a rewritten version + full test suite with **Thai comments**, no install / no lint / not run (student's explicit constraints). Rewrite extracted pure selection logic + duplicated 409/delete-error rules into unit-tested utilities, a shared modal shell, query-key factory; tests use MSW + an in-memory fake DB, a `matchMedia` shim for antd breakpoints, and an axios-instance swap to drop auth interceptors. **Writing role-first queries against the real code surfaced 3 real a11y bugs**: `Form.Item` without `name`/`htmlFor` → labels not linked; every "select whole group" checkbox had the same accessible name (fix: `role="group"` + `aria-labelledby` → `within(group)`); loading skeleton with no `aria-busy`; plus a low-risk `Record[code]` prototype-key lookup (`"constructor"`). **Say-back Q1** (`setTimeout(500)` "fix"): **pass on concept** (getBy = now, findBy = will appear); precision miss — said the sleep "waits till the thing is really there" (it waits a fixed 500 ms regardless → flaky on slow CI, slow on fast responses; same real-time lesson as 7.2's clock). **Q2** (keep MSW vs `vi.mock('./useRepoStats')`): "no idea" → fill-in-the-blank with **short-label** options → 4/4 correct → **composed own sentence, both payers named in one pass** (team debugs, users) — but **meaning inversion: "users that cause the bugs"** (users *hit* bugs); also "code change" too weak (should be "broken"), missing the false-confidence → merged → production link. Pass with corrections; added the second direction (mock also false-alarms on hook refactor). **One-line repeat of the tightened version skipped 4× in a row** → asked "keep or drop the repeat step?" — **unanswered** when student ran `/done`. English: subject–verb agreement again (*test pass → passes*, *cost land → lands*), *find where bug is it*. |
| **Previous Session Summary** | **7.2 Unit Testing (Vitest)** — taught in two parts with mechanics (testing is new ground), grounded in Repo Pulse: why Vitest (shared Vite pipeline), `describe`/`it`/`expect` + matcher traps, **`it.each` boundary tables** (a `formatCount` returning `"1000k"` at 999,950), schema test pinning an architecture decision, watch mode; isolation (`beforeEach`/`afterEach`), injected `vi.fn()` as dependency injection, `vi.mock` hoisting + the mocking-your-internals warning, fake timers, reading coverage, Goodhart's law. Q1 (clock-dependent test) pass unaided with a volunteered who-pays; Q2 (90% coverage gate) needed MC → first genuinely re-worded full answer → who-pays half-complete → coached to both payers. |
| **Homework** | None open. 7.1 test plan + 7.2 hands-on were **skipped at the student's request (2026-09-14)** and are not to be re-collected. No homework set for 7.3 — the student chose a written worked example on real work code instead. |
| **Homework Status** | ⏭️ Skipped (7.1, 7.2) · none set (7.3). Consequence: Module 07 milestone = first executed test code → review closely (tests assert outcomes not calls; boundary fixes aren't special-cased; role-first queries; network faked at the boundary). |
| **Next Action** | **1) Ask first:** keep or drop the one-line repeat of the tightened version? (skipped 4× in a row; question left unanswered at `/done`). If dropped, update memory `lighter-say-back-drill` + `me.md` cadence line. **2) Teach 7.4 E2E Testing (Playwright):** what E2E is for (the 1–3 critical flows only — callback to the pyramid rule), Playwright locators are the same role-first idea (`page.getByRole` — the RTL habit transfers), **auto-waiting + web-first assertions (`await expect(locator).toBeVisible()`) → no sleeps** (directly revisit the Q1 precision gap: a fixed sleep is a guess, a condition wait is not), test isolation (fresh browser context, `storageState` for auth), real backend vs `page.route` mocking trade-off at the E2E level, flakiness sources, trace viewer, running in CI. Natural subjects: Repo Pulse search → dashboard happy path; the roles admin create → appears in table → delete flow. Cadence: architect altitude → **2** say-back questions; draft-then-polish; skeleton up front; only the last pushed for who-pays. **Articulation playbook:** fill-in-the-blank → **short-label** multiple-choice if stalled (worked cleanly in 7.3: 4/4 and the sentence was composed, not copied) → say the whole thing back. **Watch-items:** (a) keep MC options as short labels; (b) who-pays now arrives with both payers but check **verb direction** (users *hit* / *pay for* bugs, not *cause* them); (c) mechanism vs goal — when asked "what would X do differently", answers drift to the goal ("keep accessibility") instead of the mechanism ("throws → red"); (d) precision on waits (fixed sleep vs condition wait); (e) English: subject–verb agreement is the most persistent slip. Module 07 milestone = Repo Pulse at production quality (unit + component + one E2E flow + a11y audit + Lighthouse pass + written testing strategy). |
